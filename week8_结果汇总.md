# 第八周作业 Demo 测试结果汇总表

**项目**：Agent 调用 RAG 知识库（ReAct 全链路）
**技术栈**：Python 3.13 · LangChain 0.3（create_react_agent + AgentExecutor）· Chroma 向量库
**模型**：DeepSeek deepseek-chat（对话）· 阿里百炼 text-embedding-v3（向量化）
**知识库**：kb_main，11 个片段（chunk=500 / overlap=50），data/ 目录 5 份文档
**完整日志**：见同目录 `week8_test_log.txt`

| # | 用户提问 | Agent 判断（Thought） | 工具调用（Action） | 检索结果（Observation 摘要） | 最终回答（Final Answer） | 验收路径 |
|---|---------|---------------------|------------------|---------------------------|------------------------|---------|
| 1 | NBA是哪一年成立的？ | 知识类问题，先检索验证 | `rag_search({"query":"NBA是哪一年成立的","top_k":3})` | [1] nba.txt（distance=0.333）：NBA 成立于 1946 年… | NBA 成立于 **1946 年 6 月 6 日**，30 支球队分东西部联盟各 15 支（依据：[1]） | ✅ 检索成功 |
| 2 | 介绍一下黑洞 | 天文科普类，可能包含，检索验证 | `rag_search({"query":"黑洞","top_k":3})` | [1] science.txt（distance=0.692）：黑洞是广义相对论预言的极端天体… | 黑洞引力强大到连光都无法逃脱，边界为"事件视界"，按质量分类（依据：[1]） | ✅ 检索成功 |
| 3 | 今天天气怎么样？ | 不确定知识库是否包含，必须先检索验证 | `rag_search({"query":"今天天气怎么样"})` | 3 条片段**全部标记【与问题相关性低】**（distance 1.222/1.246/1.251） | **「资料不足，无法从知识库中找到相关内容。」** 并说明库内只有天文/NBA 主题 | ✅ 检索无结果 → 拒答 |
| 4 | 知识库里有什么内容？ | 状态类问题，调用 rag_status | `rag_status({})` | collection=kb_main · 11 个片段 · data/ 5 份文档 · uploads/ 不存在 | 汇报片段数、就绪状态与文档主题清单 | ✅ 多工具路由 |
| 5 | 你好，你是谁？ | 闲聊/身份提问，无需工具 | **（未调用任何工具）** | — | 声明只回答知识库内文档相关问题，并引导提问 | ✅ 不调用工具 |

## 三条验收路径覆盖情况

| 验收标准 | 对应用例 | 结论 |
|---------|---------|------|
| 路径一：检索成功，结合片段回答并注明依据 | 用例 1、2 | ✅ 回答末尾均带（依据：[N]） |
| 路径二：检索无结果，明确拒答不编造 | 用例 3 | ✅ 精确输出规定拒答话术 |
| 路径三：与知识库无关，不调用工具 | 用例 5 | ✅ 声明职责边界 |
| 附加：多工具路由（rag_search / rag_status） | 用例 4 | ✅ |

## ReAct 闭环证据（用例 3 节选自 verbose 日志）

```
Thought: 用户问的是天气，属于不确定知识库是否包含的主题，
         按行为准则必须先调用 rag_search 检索验证
Action:  rag_search
Action Input: {"query": "今天天气怎么样"}
Observation: [1] …【与问题相关性低】 [2] …【与问题相关性低】 [3] …【与问题相关性低】
Thought: 检索返回的 3 条片段全部标记为"与问题相关性低"…资料不足，不能强行作答
Final Answer: 资料不足，无法从知识库中找到相关内容。
```

## 关键设计点

1. **工具只检索不生成**：`rag_search` 内部纯调用第 5 周 `RagPipeline.retrieve_with_score()`，绝不调用 LLM，答案完全由 Agent 结合片段生成
2. **相关性标记机制**：distance > 1.1 的片段自动标注【与问题相关性低】，为 Agent 的拒答判断提供明确依据
3. **多参数 ReAct 解析器**：继承官方解析器，将 Action Input 的 JSON 解析为 dict，按具名参数绑定 `query/top_k`（复用第 7 周方案）
4. **异常友好化**：建库失败/接口异常统一转中文错误信息返回给 Agent 观察，不抛异常中断链路
